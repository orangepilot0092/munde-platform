services:
  db:
    image: pgvector/pgvector:pg16
    container_name: mundhe-db
    environment:
      POSTGRES_USER: sahyadri
      POSTGRES_PASSWORD: mundhe_dev_password
      POSTGRES_DB: mundhe_core
    ports:
      - "5432:5432"
    volumes:
      - mundhe_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sahyadri"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: mundhe-minio
    environment:
      MINIO_ROOT_USER: mundhe_minio
      MINIO_ROOT_PASSWORD: mundhe_minio_password
    ports:
      - "9000:9000"
      - "9001:9001"
    command: server /data --console-address ":9001"
    volumes:
      - /mnt/d/sahyadri-data/minio:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: mundhe-redis
    ports:
      - "6379:6379"
    volumes:
      - mundhe_redis_data:/data

volumes:
  mundhe_db_data:
  mundhe_redis_data:
